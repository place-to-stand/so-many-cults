import band from "@/data/band.json";

export const BAND_NAME = band.name;
export const BAND_SUBTITLE = band.subtitle;
export const BAND_CITY = band.city;
export const BAND_EMAIL = band.email;
export const BAND_WEBSITE = band.website;
export const SITE_URL = band.siteUrl;

export const logo = band.logo;
export const members = band.members;
export const ffo = band.ffo;
export const shortBio = band.shortBio;
export const extendedBio = band.extendedBio;
